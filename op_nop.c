#include "monty.h"

/**
 * op_nop - does nothing
 * @stack: pointer to pointer
 * @line_number: line number
 */

void op_nop(stack_t **stack, unsigned int line_number)
{
	UNUSED(stack);
	UNUSED(line_number);
}
